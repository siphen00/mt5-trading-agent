# Connector (Windows machine, where MT5 terminal runs)
MetaTrader5
pandas
numpy
requests
python-dotenv

# Journal scripts (daily_ollama.py, weekly_review.py) only need requests —
# both run entirely against your local Ollama instance, no paid API.
